## What changed and why

<!-- One or two sentences. Link the issue if there is one. -->

## The run

<!-- Paste the real `stm` summary line (or --json) from the fixture or your app after this change. -->

```
```

## Checklist

- [ ] `npm test && npm run check && npm run lint` pass locally
- [ ] If `src/capture.js` changed: ran the fixture twice, second run reports `0 changed`
- [ ] One line under `## [Unreleased]` in `CHANGELOG.md`
- [ ] No new runtime dependency, flag, or command (or an issue approved it first)
- [ ] I understand every line of this change (say below if an AI agent wrote most of it)
